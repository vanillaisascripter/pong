export class Controls {
  constructor(canvas, physics) {
    this.canvas = canvas;
    this.physics = physics;
  }

  init() {
    // Add touch/mouse/keyboard event listeners
    // Update paddle positions in physics.state
  }
}
